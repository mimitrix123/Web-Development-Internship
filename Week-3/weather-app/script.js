const API_KEY = 'YOUR_OPENWEATHERMAP_API_KEY';
const form = document.getElementById('weatherForm');
const status = document.getElementById('status');
const weather = document.getElementById('weather');
form.addEventListener('submit', async (event) => { event.preventDefault(); const city=document.getElementById('city').value.trim(); if(!city)return; if(API_KEY==='YOUR_OPENWEATHERMAP_API_KEY'){status.textContent='Add your OpenWeatherMap API key in script.js to enable live weather data.'; return;} status.textContent='Loading...'; weather.hidden=true; try { const response=await fetch(`https://api.openweathermap.org/data/2.5/weather?q=${encodeURIComponent(city)}&appid=${API_KEY}&units=metric`); if(!response.ok) throw new Error(response.status===404?'City not found.':'Unable to fetch weather data.'); const data=await response.json(); document.getElementById('location').textContent=`${data.name}, ${data.sys.country}`; document.getElementById('temperature').textContent=Math.round(data.main.temp); document.getElementById('humidity').textContent=data.main.humidity; document.getElementById('condition').textContent=data.weather[0].description; weather.hidden=false; status.textContent=''; } catch(error){status.textContent=error.message;} });